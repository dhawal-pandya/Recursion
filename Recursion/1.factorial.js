// num = 5

// 5 * 4 * 3 * 2 * 1 = 120;

const num = 5;

const fac = (n) => {
  // // Iteration
  //
  // let factorial = 1;
  // for (let i = n; i > 0; i--) {
  //   factorial *= i;
  // }
  // return factorial;
  //
  // //  Recursion
  //
  // if (n === 1) return 1;
  // return n * fac(n - 1);
};

console.log(fac(num));
