let num = 5; // goes exponent above 102, goes apeshit above 1476

const fib = (n) => {
  // // Iteration
  //
  // let fibArr = [1, 1];
  // for (i = 0; i < n - 2; i++) {
  //   let m = fibArr[i] + fibArr[i + 1];
  //   fibArr.push(m);
  // }
  // //
  // for (let i = 0; i < fibArr.length; i++) {
  //   console.log(i + 1, fibArr[i]);
  // }
  //
  // // Recursion
  //
  // if (n <= 1) return 1;
  // return fib(n - 1) + fib(n - 2);
};

fib(num);

// for (let i = 0; i < num; i++) {
//   console.log(i + 1, fib(i));
// } // uncomment for the recursion
